{% set actual_challenge = challenges|first %}
<div class="px-4 pt-5 my-1 text-center">
  <h1 class="display-4 fw-bold">{{ 'challenge.index.header.title'|trans }}</h1>
  <div class="col-lg-6 mx-auto">
    <p class="lead mb-4">Quickly design and customize responsive mobile-first sites with Bootstrap, the world’s most popular front-end open source toolkit, featuring Sass variables and mixins, responsive grid system, extensive prebuilt components, and powerful JavaScript plugins.</p>
    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mb-5">
    {% if app.user %}
      <a href="{{ path('challenge_show', {'id': actual_challenge.id})}}" class="btn btn-primary btn-lg px-4 me-sm-3">{{ 'challenge.index.header.cta.last_challenge'|trans }}</a>
    {% else %}
      <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg px-4">{{ 'challenge.index.header.cta.register'|trans }}</a>
    {% endif %}
    </div>
  </div>
  <div class="overflow-hidden" style="max-height: 30vh;">
    <div class="container px-5">
      <img src="https://www.fillmurray.com/640/360" class="img-fluid rounded-3 shadow-lg" alt="Example image" width="700" height="500" loading="lazy">
    </div>
  </div>
</div>