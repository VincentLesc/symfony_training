{% extends 'base_admin.html.twig' %}

{% block title %}ChallengeParticipation{% endblock %}

{% block body %}
    <h1>ChallengeParticipation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ challenge_participation.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ challenge_participation.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ challenge_participation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('challenge_admin_challenge_participation_index') }}">back to list</a>

    <a href="{{ path('challenge_admin_challenge_participation_edit', {'id': challenge_participation.id}) }}">edit</a>

    {{ include('challenge/admin/challenge_participation/_delete_form.html.twig') }}
{% endblock %}
