<form method="post" action="{{ path('challenge_admin_challenge_participation_delete', {'id': challenge_participation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ challenge_participation.id) }}">
    <button class="btn">Delete</button>
</form>
